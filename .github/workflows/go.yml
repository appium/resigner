# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.26'

    - name: Check formatting
      run: |
        if [ -n "$(gofmt -s -l cmd pkg)" ]; then
          echo "Go code is not formatted. Please run 'gofmt -s -w cmd pkg' to format the code."
          exit 1
        fi

    - name: Test
      run: go test -v ./...

    - name: Build
      run: make all
